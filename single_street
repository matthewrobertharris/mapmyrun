import osmnx as ox

address = "45 Queens Road, New Lambton, NSW, 2305, Australia"

# Search and get the street geometry
gdf = ox.features_from_place(address, tags={"highway": True})

# Filter to just the named street
gdf_street = gdf[gdf['name'] == 'Queens Road']

# Plot it
gdf_street.plot()